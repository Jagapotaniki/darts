#include <stdio.h>
#include <stdbool.h> 

int main(void) {

    bool target[20] = {false};
    int multiple = 1;
    int round, start;

    while(1) {
        printf( "1:Start, 0:Exit" );
        scanf( "%d", &start );
        if( start == 1 ) {

        } else if( start == 0 ) {

        }
    }
}